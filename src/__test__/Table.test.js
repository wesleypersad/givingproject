import { render, screen } from '@testing-library/react';
import Table from '../components/Table';
import Admin from '../components/Table';

test('expect table function to exist', () => {
    expect(Table).toBeDefined();
});
